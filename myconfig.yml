server:
  port: 1234
spring:
    application:
        name: eurekaClient
eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
management:
    security:
        enable: false
